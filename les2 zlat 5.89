# Создаем переменную для хранения количества чисел
count_numbers = 0

# Используем цикл for для перебора всех трехзначных чисел
for i in range(100, 1000):
  # Проверяем, является ли число кратным семи
  if i % 7 == 0:
    # Считаем сумму цифр числа
    number_str = str(i)
    sum_digits = 0
    for digit in number_str:
      sum_digits += int(digit)
    # Проверяем, является ли сумма цифр числа кратной семи
    if sum_digits % 7 == 0:
      count_numbers += 1
    
# Выводим результат
print("Количество всех трехзначных чисел, кратных семи и у которых сумма цифр также кратна семи: ", count_numbers)

