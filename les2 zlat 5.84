# Создаем переменную для хранения суммы всех четных трехзначных чисел
sum_even_numbers = 0

# Используем цикл for для перебора всех трехзначных чисел
for i in range(100, 1000):
  # Проверяем, является ли число четным
  if i % 2 == 0:
    sum_even_numbers += i
    
# Выводим результат
print("Сумма всех четных трехзначных чисел: ", sum_even_numbers)
