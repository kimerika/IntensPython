import math
# Запрос высоты у пользователя
height = float(input("Введите высоту, с которой будет отпущен объект: "))
# Расчет скорости падения объекта
acceleration = 9.8 # Ускорение свободного падения
velocity = round(math.sqrt(2 * acceleration * height), 1)
# Вывод результата
print("Объект, отпущенный с высоты %.2f м, врежется в землю со скоростью %.1f м/с." % (height, velocity))
