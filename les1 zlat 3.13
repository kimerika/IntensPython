p = int(input("Введите порядковый номер: "))
if p < 1 or p > 50:
    print("Такого порядкового номера нет в таблице")
else:
    row = (p - 1) // 5 + 1
    column = (p - 1) % 5 + 1
    print(f"Значение находится в строке {row}")
    print(f"Значение находится в столбце {column}")

