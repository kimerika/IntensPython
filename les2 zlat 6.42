number = input("Введите натуральное число: ") # вводим число с клавиатуры

# определяем максимальную цифру в числе
max_digit = max(number)
# определяем минимальную цифру в числе
min_digit = min(number)

# выводим результат на экран
print("Максимальная цифра в числе:", max_digit)
print("Минимальная цифра в числе:", min_digit)

# преобразуем цифры в число и вычисляем разницу между ними
diff = int(max_digit) - int(min_digit)
print("Разница между максимальной и минимальной цифрой:", diff)

# вычисляем сумму максимальной и минимальной цифр
sum_digits = int(max_digit) + int(min_digit)
print("Сумма максимальной и минимальной цифр:", sum_digits)
