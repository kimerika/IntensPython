# Получаем значение переменной 'a' от пользователя с помощью функции input
a = int(input("Введите значение переменной 'a': "))

# Используем цикл для подсчета суммы всех чисел от 'a' до 400
sum = 0
for i in range(a, 401):
    sum += i

print("Сумма всех целых чисел от", a, "до 400: ", sum)
