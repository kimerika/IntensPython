g = float(input("Введите первый член прогрессии: "))
z = float(input("Введите знаменатель прогрессии: "))
t = float(input("Введите число: "))

if z == 1:
    if g == t:
        print(t, "является членом прогрессии")
    else:
        print(t, "не является членом прогрессии")
else:
    n = math.log(t / g, z)
    if n == int(n):
        print(t, "является членом прогрессии")
    else:
        print(t, "не является членом прогрессии")
