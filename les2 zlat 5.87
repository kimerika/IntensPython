# Создаем переменную для хранения количества чисел
count_numbers = 0

# Используем цикл for для перебора всех чисел в диапазоне от 100 до 500
for i in range(100, 501):
  # Переводим число в строку, чтобы посчитать сумму цифр
  number_str = str(i)
  # Считаем сумму цифр числа
  sum_digits = 0
  for digit in number_str:
    sum_digits += int(digit)
  # Проверяем равенство суммы цифр 15
  if sum_digits == 15:
    count_numbers += 1
    
# Выводим результат
print("Количество натуральных чисел в интервале от 100 до 500, сумма цифр которых равна 15: ", count_numbers)
