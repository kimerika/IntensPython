# Запрашиваем предложение и символ
sentence = input("Введите предложение: ")
char = input("Введите символ: ")

# Используем цикл для перебора всех символов в предложении
for letter in sentence:
    # Проверяем, является ли текущий символ заданным символом
    if letter == char:
        # Выводим символ на отдельной строке
        print(char)
