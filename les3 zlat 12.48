# Вводим исходные слова
word1 = input("Введите первое слово: ")
word2 = input("Введите второе слово (короче первого): ")

# Вычисляем количество символов, которые нужно заменить во втором слове
num_chars = len(word2)

# Формируем новое слово, заменив символы второго слова на символы первого слова
new_word2 = word1[:num_chars] + word2[num_chars:]

# Выводим на экран полученный результат
print("Результат замены: ", new_word2)
