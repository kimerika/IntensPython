# Создаем список стоимостей легковых автомобилей и мотоциклов
costs = [7000, 6000, 8000, 3500, 4000, 5000, 5500, 3000, 4500, 9000, 700, 1000, 650, 800, 900]

# Отбираем только стоимости автомобилей
car_costs = [x for x in costs if x > 5000]

# Отбираем только стоимости мотоциклов
bike_costs = [x for x in costs if x <= 5000]

# Если нет мотоциклов, выходим из программы
if len(bike_costs) == 0:
  print("Нет данных о стоимости мотоциклов")
  exit()

# Если нет автомобилей, выходим из программы
if len(car_costs) == 0:
  print("Нет данных о стоимости автомобилей")
  exit()

# Вычисляем средние стоимости автомобилей и мотоциклов
average_car_cost = sum(car_costs) / len(car_costs)
average_bike_cost = sum(bike_costs) / len(bike_costs)

# Проверяем соотношение средних стоимостей
if average_car_cost / average_bike_cost > 3:
  print("Средняя стоимость автомобилей превышает среднюю стоимость мотоциклов более чем в 3 раза")
else:
  print("Средняя стоимость автомобилей не превышает среднюю стоимость мотоциклов более чем в 3 раза")
