height = float(input("Введите свой рост (в сантиметрах или дюймах): "))
weight = float(input("Введите свой вес (в килограммах или фунтах): "))
if height < 100: # Если пользователь ввел рост в дюймах
    bmi = (weight / (height**2)) * 703
else: # Если пользователь ввел рост в сантиметрах
    bmi = weight / (height**2)
print("Ваш индекс массы тела (BMI) составляет:", round(bmi, 2))
