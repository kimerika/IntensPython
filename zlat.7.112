# Создаем список чисел
numbers = [3, 17, 8, 12, 6, 13, 5, 7, 22, 9, 10, 18, 11, 15, 16]

# Фильтруем числа больше 10
filtered_numbers = [x for x in numbers if x > 10]

# Проверяем, есть ли числа больше 10 в списке
if len(filtered_numbers) == 0:
  print("В списке нет чисел, больших 10")
else:
  # Вычисляем среднее арифметическое
  average = sum(filtered_numbers) / len(filtered_numbers)

  # Выводим результат
  print("Среднее арифметическое тех чисел, которые больше 10: ", average)
