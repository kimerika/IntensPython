# Получаем пользовательский ввод для a и b
a = int(input("Введите начальное значение промежутка: "))
b = int(input("Введите конечное значение промежутка: "))

# Создаем переменную для хранения суммы всех чисел, кратных четырем
sum_multiple_of_four = 0

# Используем цикл for для перебора всех чисел в диапазоне от a до b
for i in range(a, b+1):
    # Проверяем, является ли число кратным четырем
    if i % 4 == 0:
        sum_multiple_of_four += i

# Выводим результат
print("Сумма всех чисел, кратных четырем: ", sum_multiple_of_four)
