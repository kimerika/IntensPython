def proper_divisors(num):
    divisors = []
    for i in range(1, num):
        if num % i == 0:
            divisors.append(i)
    return divisors

if name == "main":
    number = int(input("Введите число: "))
    divisors = proper_divisors(number)
    print(f"Собственные делители числа {number}: {divisors}")
