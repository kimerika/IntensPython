# Создаем список целых чисел
numbers = [2, 5, 1, 7, 4, 6, 8, 10, 3, 9, 11, 12, 21, 16, 19]

# Фильтруем только четные числа
even_numbers = [num for num in numbers if num % 2 == 0]

# Проверяем, есть ли четные числа в списке
if len(even_numbers) == 0:
  print("В списке нет четных чисел")
else:
  # Вычисляем среднее арифметическое
  average = sum(even_numbers) / len(even_numbers)

  # Выводим результат
  print("Среднее арифметическое четных чисел: ", average)
