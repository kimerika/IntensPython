а) 
number = input("Введите четырехзначное число: ")

reverse_number = number[::-1]

print("Число, полученное при прочтении исходного числа справа налево:", reverse_number)

б)


number = input('Введите четырехзначное число: ')

if len(number) != 4:
    print('Вы ввели неверное число цифр')
else:
    # преобразуем число в список, чтобы его можно было изменять
    digits = list(number)
    
    # меняем местами первую и вторую цифру
    digits[0], digits[1] = digits[1], digits[0]
    
    # меняем местами третью и четвертую цифру
    digits[2], digits[3] = digits[3], digits[2]
    
    # преобразуем список обратно в строку и выводим результат
    new_number = ''.join(digits)
    print('Число, образуемое при перестановке цифр:', new_number)

в1)
# Запрашиваем у пользователя четырехзначное число
number = input('Введите четырехзначное число: ')

# Проверяем, что введенное значение является четырехзначным числом
if len(number) != 4 or not number.isdigit():
    print('Вы ввели неверное число')
else:
    # Получаем отдельные цифры введенного числа
    thousands = int(number[0])
    hundreds = int(number[1])
    tens = int(number[2])
    ones = int(number[3])

    # Меняем местами вторую и третью цифры
    tens, hundreds = hundreds, tens

    # Выводим новое число, образованное после перестановки цифр
    new_number = str(thousands) + str(tens) + str(hundreds) + str(ones)
    print('Новое число:', new_number)
в2)
# Запрашиваем у пользователя четырехзначное число
number = input('Введите четырехзначное число: ')

# Проверяем, что введенное значение является четырехзначным числом
if len(number) != 4 or not number.isdigit():
    print('Вы ввели неверное число')
else:
    # Преобразуем число в строку и заменяем символы второй и третьей позиции местами
    new_number = number[:1] + number[2:3] + number[1:2] + number[3:]

    # Выводим новое число, образованное после перестановки цифр
    print('Новое число:', new_number)

г1)
# Запрашиваем у пользователя четырехзначное число
number = input('Введите четырехзначное число: ')

# Проверяем, что введенное значение является четырехзначным числом
if len(number) != 4 or not number.isdigit():
    print('Вы ввели неверное число')
else:
    # Преобразуем число в строку и заменяем символы местами
    new_number = number[2:] + number[:2]

    # Выводим новое число
    print('Новое число:', new_number)

г2)
# Запрашиваем у пользователя четырехзначное число
number = input('Введите четырехзначное число: ')

# Проверяем, что введенное значение является четырехзначным числом
if len(number) != 4 or not number.isdigit():
    print('Вы ввели неверное число')
else:
    # Преобразуем число в строку и заменяем символы местами
    new_number = number[2:] + number[:2]

    # Выводим новое число
    print('Новое число:', new_number)
