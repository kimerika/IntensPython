sentence = "Дано предложение. Определить, сколько в нем одинаковых соседних букв."
count = 0

# Приводим все символы к строчным, чтобы искать как заглавные, так и строчные буквы
sentence = sentence.lower()

for i in range(len(sentence) - 1):
    # Если найдено повторение буквы, увеличиваем счетчик
    if sentence[i] == sentence[i + 1]:
        count += 1

print("Число одинаковых соседних букв в предложении:", count)
