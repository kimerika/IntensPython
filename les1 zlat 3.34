# Находим неизвестное число х по формуле
# (оставшееся число * 10) + первая цифра числа x = 564
# Затем объединяем первую цифру и найденное число x, чтобы получить исходное число
result = 564
x = (result // 10)
first_digit = result - x * 10
number = int(str(first_digit) + str(x))

# Выводим результат
print("Число x равно: ", number)
