number = int(input("Введите натуральное число: ")) # вводим число с клавиатуры
first_digit = int(str(number)[0]) # определяем первую цифру числа

count = 0 # инициализируем счетчик вхождений первой цифры

# проходим по каждой цифре числа и проверяем, совпадает ли она с первой цифрой
for digit in str(number):
    if int(digit) == first_digit:
        count += 1
        
# выводим результат на экран
print("Первая цифра в числе встречается", count, "раз(а)")
