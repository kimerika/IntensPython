# Задаем исходное трехзначное число
number = int(input("Введите трехзначное число: "))

# Вычисляем последнюю цифру, вычитаем ее из числа и получаем новое значение
last_digit = number % 10
new_number = number - last_digit

# Делим новое значение на 10 и прибавляем к нему число x, умноженное на 100
# Затем к результату дописываем последнюю цифру, которую мы вычли из исходного числа
p = int(str(new_number // 10) + str(last_digit))
x = (p - 237) // 100

# Выводим результат
print("Число p равно: ", p)
print("Число x равно: ", x)
