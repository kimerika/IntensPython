# Функция для расчета значения y для заданных x
def calculate_y(x):
    t = x + 3
    y = 3 * t ** 2 + 4.87 * t - 3
    return y

# Вычисляем значения y для x от 4 до 28
for x in range(4, 29):
    y = calculate_y(x)
    print("Значение y для x =", x, "равно", y)
