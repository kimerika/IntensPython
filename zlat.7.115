# Задаем значения т и р
t = 10
p = 3

# Создаем список целых чисел
numbers = [2, 5, 1, 7, 4, 6, 9, 10, 12, 11, 15]

# Фильтруем числа, которые кратны п
filtered_numbers = [num for num in numbers if num % p == 0]

# Проверяем, есть ли числа, кратные p, в списке
if len(filtered_numbers) == 0:
  print("В списке нет чисел, кратных ", p)
else:
  # Вычисляем среднее арифметическое
  average = sum(filtered_numbers) / len(filtered_numbers)

  # Выводим результат
  print("Среднее арифметическое тех чисел, которые кратны ", p, ": ", average)
