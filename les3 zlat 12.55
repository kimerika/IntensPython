sentence = "Дано предложение. Вывести все буквы, стоящие на четных местах, «столбиком»"
even_letters = ""

for i in range(len(sentence)):
    # Так как отсчет в Python начинается с нуля, первая буква будет с четным индексом 0
    if i % 2 == 0:
        even_letters += sentence[i] + "\n" # Добавляем перенос строки, чтобы выводить буквы столбиком

print(even_letters)
