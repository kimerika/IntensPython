V1 = float(input("Введите скорость первого автомобиля (км/ч): "))
V2 = float(input("Введите скорость второго автомобиля (км/ч): "))
S = float(input("Введите расстояние между автомобилями (км): "))

# Определяем время, которое потребуется первому автомобилю, чтобы опередить второй
time = S / (V1 - V2)

# Рассчитываем расстояние между автомобилями через 30 минут после опережения
distance = (V1 * time + 0.5 * (V1 - V2) * 0.5 * time**2) - (V2 * 0.5)

print("Расстояние между автомобилями через 30 минут после опережения: ", distance, "км")
