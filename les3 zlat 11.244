# а) в том же порядке расположения элементов
arr = [1, 2, 3, 4, 5]
new_arr = arr.copy()
print(new_arr)

# б) в обратном порядке расположения элементов
arr = [1, 2, 3, 4, 5]
new_arr = arr[::-1].copy()
print(new_arr)
