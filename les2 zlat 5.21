# Запрос ввода стоимости 1 кг сыра
price_per_kg = float(input("Введите стоимость 1 кг сыра: "))

# Выводим заголовок таблицы
print("Вес (г)  |  Цена")

# Переменные для хранения веса и стоимости сыра
weight = 50
total_price = 0

# Вычисляем стоимость для каждого веса в таблице
while weight <= 1000:
    # Вычисляем стоимость для текщего веса
    price = round(price_per_kg * weight / 1000, 2)
    total_price += price
     
    # Выводим строку таблицы
    print("{:6d}   | {:6.2f} руб.".format(weight, price))
     
    # Увеличиваем вес на 50 граммов
    weight += 50

# Выводим общую стоимость
print("Общая стоимость: {:6.2f} руб.".format(total_price))
